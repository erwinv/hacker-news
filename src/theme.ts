import { extendTheme } from '@mui/joy'

export default extendTheme({
  components: {
    JoyLink: {
      defaultProps: {
        color: 'neutral',
      },
    },
    JoyButton: {
      defaultProps: {
        color: 'neutral',
      },
    },
    JoyIconButton: {
      defaultProps: {
        color: 'neutral',
      },
    },
    JoyBadge: {
      defaultProps: {
        color: 'neutral',
      },
    },
    JoyCircularProgress: {
      defaultProps: {
        color: 'neutral',
      },
    },
    JoyLinearProgress: {
      defaultProps: {
        color: 'neutral',
      },
    },
  },
})
